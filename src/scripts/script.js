// const width = 800;
// const height = 500;

// const svg = d3.select("#map-container")

// .append("svg").attr("width", width).attr("height", height)

// d3.json("word.geojson").then(function (world) {
//     const project = d3.geoMercator()
//     .scale(150).translate([width / 2, height / 2]);
// })